services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=garimpo-db'
      - 'POSTGRES_USER=postgres'
      - 'POSTGRES_PASSWORD=password'
    ports:
      - '5432:5432'
